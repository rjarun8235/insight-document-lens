import { supabase } from "@/integrations/supabase/client";

// The fetchApiKeyFromSupabase function has been removed since we're now using
// the claude-api-proxy Edge Function which handles the API key securely on the server side
